{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:crewfold:schema:local-api:approval-list-params:v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["workspace"],
  "properties": {
    "workspace": {"type": "string", "pattern": "^ws_[0-9a-f]{32}$"},
    "project": {"type": "string", "pattern": "^prj_[0-9a-f]{32}$"},
    "status": {"enum": ["pending", "granted", "denied", "expired", "consumed"]},
    "action": {"type": "string", "pattern": "^saction_[0-9a-f]{32}$"},
    "cursor": {"$ref": "operator-page.schema.json#/$defs/cursor"},
    "limit": {"$ref": "operator-page.schema.json#/$defs/limit"}
  }
}
