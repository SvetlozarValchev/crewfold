{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:crewfold:schema:local-api:run-stop-params:v1",
  "title": "Crewfold local API run stop parameters v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["workspace", "run", "expected_revision", "grace_period_millis", "idempotency_key"],
  "properties": {
    "workspace": {"type": "string", "minLength": 1, "maxLength": 128},
    "run": {"type": "string", "pattern": "^run_[0-9a-f]{32}$"},
    "expected_revision": {"type": "integer", "minimum": 1},
    "grace_period_millis": {"type": "integer", "minimum": 1, "maximum": 30000},
    "idempotency_key": {"type": "string", "minLength": 1, "maxLength": 128}
  }
}
