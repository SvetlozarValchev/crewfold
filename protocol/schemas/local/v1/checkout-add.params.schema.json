{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:crewfold:schema:local-api:checkout-add-params:v1",
  "title": "Crewfold local API checkout add parameters v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["workspace", "project", "repository_path", "idempotency_key"],
  "properties": {
    "workspace": {"type": "string", "minLength": 1, "maxLength": 128},
    "project": {"type": "string", "minLength": 1, "maxLength": 128},
    "repository_path": {"type": "string", "minLength": 1},
    "write_mode": {"enum": ["exclusive", "claimed", "shared", "read_only"]},
    "idempotency_key": {"type": "string", "minLength": 1, "maxLength": 128}
  }
}
