{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:crewfold:schema:domain:knowledge-contradiction-list:v1",
  "title": "Crewfold bounded knowledge contradiction list v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["details"],
  "properties": {
    "details": {
      "type": "array",
      "maxItems": 200,
	  "description": "One coherent bounded first slice in deterministic reported_at descending then ID descending order (newest first); this version has no continuation cursor.",
      "items": {"$ref": "knowledge-contradiction-detail.schema.json"}
    }
  }
}
