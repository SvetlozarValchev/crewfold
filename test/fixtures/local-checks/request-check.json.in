{
  "schema": "urn:crewfold:schema:fixture:fake-run-scenario:v1",
  "name": "local-checks-exact-granted-request",
  "acceptance": {
    "required_evidence": ["exact_check_requested"]
  },
  "steps": [
    {
      "kind": "completion",
      "message": "Requested only the requirement frozen into the exact current-packet grant.",
      "evidence": ["exact_check_requested"],
      "handoff": "The durable check worker now owns the exact allowlisted command."
    }
  ],
  "check_watch": {
    "run_requirement_id": "@REQUIREMENT@",
    "list_results": true
  }
}
