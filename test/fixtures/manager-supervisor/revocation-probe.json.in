{
  "schema": "urn:crewfold:schema:fixture:fake-run-scenario:v1",
  "name": "manager-supervisor-live-grant-revocation",
  "acceptance": {
    "required_evidence": ["revocation_enforced"]
  },
  "steps": [
    {
      "kind": "completion",
      "message": "A proposal call after owner revocation was denied.",
      "evidence": ["revocation_enforced"],
      "handoff": "The live packet-v5 snapshot did not outlive its current grant."
    }
  ],
  "management": {
    "proposals": [
      {
        "kind": "assignment",
        "summary": "Submit once before revocation and probe the same authority afterward.",
        "actions": [
          {
            "type": "assign_task",
            "assign_task": {
              "task": {"task_id": "@TARGET_TASK@", "expected_task_revision": @TARGET_TASK_REVISION@},
              "launch_profile_id": "@IMPLEMENT_PROFILE@"
            }
          }
        ]
      }
    ],
    "probe_revoked_grant": true,
    "revocation_probe_delay_millis": 3000
  }
}
