# Accepted contact ordering contract v2

Contacts are ordered by normalized family name, normalized given name, locale,
and finally by the stable contact identifier. This revision explicitly replaces
the original ordering contract while preserving its durable history.
